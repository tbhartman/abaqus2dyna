language: python
python:
  - "3.6"

install:
  - pip install .[test]
before_script:
  - pip install python-coveralls
  - mkdir coverage
script:
  - pytest --cov=abaqus2dyna
after_success:
  - coveralls --data_file coverage/.coverage
